Pøíkazy:

	run -> run simulation
	stop -> stop simulation
	key -> attach keyboard to procesor
	
	hvar -> highlite variables
	hvare -> highlite variable sector end
	hjmp -> highlight lines vith jumps
	hall -> highlight all things in code
	clsr -> resfrsh comand promt (UI)

	step -> do 1 instruction (ticks to 1 clock cycle)
	clock xx -> set haw many tick is needet to one instruction
	peek xx -> show content of register
	mv xxx xxx -> move reg source to reg dest





Instrukèní registry k pøidìlání:
	INC_A			 //pøidá jednu
	INC_OUT          //sem
	DEC_A			 //pøidá jednu
	DEC_OUT          //sem



Pøidání:
	DBGINFO: ??
		pøekladaè do souboru napíš dodateèné informace pro debug a simulator ho vezme do uvahu
		v souboru to bude vypadat takto

		50 m[AHOJ]s[200]  // na lince 50 se zmení rychlost na 200 a vypíše se AHOJ
		60 m[text]v[20]    //na lince 60 se vypíše text a obsah registru 20